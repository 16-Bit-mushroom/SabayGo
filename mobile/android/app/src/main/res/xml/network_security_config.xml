<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 9+ blocks plaintext HTTP by default, so a debug build cannot
  reach a FastAPI server running on the development machine over the LAN.

  Cleartext is permitted only for the specific development hosts below.
  Everything else still requires HTTPS, so this does not weaken the app
  generally, and production traffic is unaffected.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">192.168.1.8</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>

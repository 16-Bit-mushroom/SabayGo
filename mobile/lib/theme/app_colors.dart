import 'package:flutter/material.dart';

class AppColors {
  // Nord Light Base (Snow Storm)
  static const Color background = Color(0xFFECEFF4);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceHover = Color(0xFFE5E9F0);

  // Nord Text (Polar Night)
  static const Color textPrimary = Color(0xFF2E3440);
  static const Color textSecondary = Color(0xFF4C566A);

  // Nord Accents (Frost)
  static const Color primaryAction = Color(0xFF5E81AC); // Deep blue
  static const Color highlight = Color(0xFF88C0D0); // Cyan
  static const Color success = Color(0xFFA3BE8C); // Green for discounts
}